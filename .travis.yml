cache: bundler
language: ruby
sudo: false

os:
  - osx
  - linux

rvm:
  - 2.4
  - 2.5
  - 2.6

before_install:
  - gem update --system
  - gem install bundler

deploy:
  skip_cleanup: true
  provider: script
  script: 'bundle exec rake coveralls:push'
  on:
    branch: master
